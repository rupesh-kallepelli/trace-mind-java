replicaCount: 1

image:
  repository: ghcr.io/github/github-mcp-server
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

github:
  token: ""
  secret: ""
  clientId: ""

docker:
  pat: ""
  username: ""
  email: ""
route:
  enabled: true
  host: ""
  tls:
    termination: edge

ingress:
  enabled: false
  className: nginx
  host: github-mcp.example.com
  path: /

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
