apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-mcp-server.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "github-mcp-server.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
