server:
  port: 8086
spring:
  application:
    name: traces_mcp
  ai:
    mcp:
      server:
#        comment below for making server STDIO
        protocol: STREAMABLE
        enabled: true
        name: traces_mcp
        version: 1.0.0
management:
  endpoints:
    web:
      exposure:
        include: "*"

zipkin:
  url: https://tracing-server-kallepelli-rupesh-dev.apps.rm1.0a51.p1.openshiftapps.com:443